version: '3.7'

services:

  web-app:
    build:
      context: ./react-app
    volumes:
      - ./nginx-frontend-local.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt
    ports:
      - 80:80
